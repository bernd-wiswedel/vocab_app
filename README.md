# WortSpaß

WortSpaß helps my kids to practice Latin and English vocabulary in
School. Most of the code, including the app name and this text, was generated by
ChatGPT. 

## Requirements

- **Python 3.13+** (see [PYTHON_VERSION_POLICY.md](PYTHON_VERSION_POLICY.md))
- Dependencies listed in `requirements.txt`

## Setup

```bash
# Ensure Python 3.13+
python check_python_version.py

# Create virtual environment
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate

# Install dependencies
pip install -r requirements.txt

# Run locally
python app.py
``` 

## Source Sheets

Each learner has their own Google Sheet with the same layout (tabs `Latein`,
`Englisch`, `Scores Latein (<Name>)`, `Scores Englisch (<Name>)`). The learner
is chosen on the login page; `/login?user=<Name>` preselects one for
bookmarking. (Sheet content is not AI'ed.)

Learners are configured in `configuration.ini`, one `[user:<Name>]` section
with the `spreadsheet_id` each — nothing about them lives in the source code.
Passwords are not in the file either: set `LOGIN_PASSWORD_<NAME>` in the
environment for every learner (e.g. `LOGIN_PASSWORD_JAKOB`); a learner without
one can only be browsed as a guest.

## Deployment

The application is deployed at:
[https://abundant-sarita-wiswedel-c1ce15bc.koyeb.app/](https://abundant-sarita-wiswedel-c1ce15bc.koyeb.app/)
